#include <stdint.h>
#include <stdlib.h>

#define XMEGA 1

typedef uint8_t byte;
#define nullptr 0

#include "hal.h"

//#define Serial.Print (void)

//#define Assert(expr) { if (!(expr)) while (1); }
//#define Assert(expr) ((void) 0)
#include "arduino.h"
